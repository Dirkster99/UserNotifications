version: 1.0.{build}

branches:
  only:
  - master

build:
  verbosity: minimal

configuration: Release

platform: Any CPU

image: Visual Studio 2019 Preview

install:
  - cmd: choco install dotnetcore-sdk --pre
